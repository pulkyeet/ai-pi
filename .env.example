# Copy to .env and fill in. Settings has no defaults for secrets, so a
# missing required value raises at startup rather than at first use.

ENVIRONMENT=dev
LOG_LEVEL=INFO
OTEL_CONSOLE_EXPORT=true

# Local Postgres (docker-compose) by default; point at Supabase in prod.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_pi

OPENROUTER_API_KEY=
EXA_API_KEY=
GITHUB_TOKEN=

# --- Quota knobs (masterplan §8.2) ---
# All unset until Phase 14 measures real per-run numbers. Do not guess these.
# RUNS_PER_USER_PER_DAY=
# GLOBAL_RUNS_PER_DAY=
# MAX_CONCURRENT_RUNS=
# RUN_BUDGET_WEIGHT=
# RUN_BUDGET_USD=
# RUN_TIMEOUT_S=
# MAX_COMPETITORS_PROFILED=
# MAX_PAGES_PER_ENTITY=
# MAX_COMMUNITY_THREADS=
